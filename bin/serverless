#!/bin/bash

MYSCRIPTS_PATH=$(cd $(dirname $0)/..; pwd)
bash $MYSCRIPTS_PATH/etc/download-anytoold.sh
ANYTOOLD_PATH=$(cd $(dirname $0)/../anytoold; pwd)

name=$(basename $0)

export ANYTOOLD_EXTDIR=$MYSCRIPTS_PATH/lib/$name
export NODEJS_VERSION=${NODEJS_VERSION:-"*"}

bash $ANYTOOLD_PATH/docker-run.sh $name "$@"

